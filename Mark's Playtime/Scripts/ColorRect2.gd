extends ColorRect

var rand = 0.0
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	pass

func _process(delta):
#	randomize()
#	rand = randf()
	rect_rotation +=.1
#		if rand >= 0.9:
#			rect_rotation += rand/600

#	rand = randf()
#	for i in 60:
#		if rand >= 0.9:
#			rect_rotation -= rand/600
